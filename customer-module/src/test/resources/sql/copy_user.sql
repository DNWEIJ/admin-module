 INSERT INTO admin_user
(id,
 version,
 account,
 change_password,
 email,
 language,
 last_visit_date,
 login_enabled,
 member_local_id,
 name,
 number_of_visits,
 password,
 personnel_status,
 member_id,
 added_by,
 added_on,
 last_edited_by,
 last_edited_on)
SELECT USER_ID,
       VERSION,
       ACCOUNT,
       false,
       EMAIL,
       LANGUAGE,
       LAST_VISIT_DATE,
       LOGIN_ENABLED,
       0,
       NAME,
       NUMBER_VISIT_DAYS,
       PASSWORD,
       PERSONNEL_TYPE,
       MEMBER_ID,
       ADDED_BY,
       ADDED_ON,
       LAST_EDITED_BY,
       LAST_EDITED_ON
FROM
    vmas.thau_user WHERE MEMBER_ID = 77;