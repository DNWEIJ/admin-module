 INSERT INTO admin_user
(id,
 version,
 account,
 change_password,
 email,
 language,
 last_visit_date,
 login_enabled,
 member_local_id,
 name,
 number_of_visits,
 password,
 personnel_status,
 member_id,
 added_by,
 added_on,
 last_edited_by,
 last_edited_on)
SELECT thau_user.USER_ID,
       thau_user.VERSION,
       thau_user.ACCOUNT,
       false,
       thau_user.EMAIL,
       thau_user.LANGUAGE,
       thau_user.LAST_VISIT_DATE,
       thau_user.LOGIN_ENABLED,
       0,
       thau_user.NAME,
       thau_user.NUMBER_VISIT_DAYS,
       thau_user.PASSWORD,
       thau_user.PERSONNEL_TYPE,
       thau_user.MEMBER_ID,
       thau_user.ADDED_BY,
       thau_user.ADDED_ON,
       thau_user.LAST_EDITED_BY,
       thau_user.LAST_EDITED_ON
FROM
    vmas.thau_user WHERE MEMBER_ID = 77;